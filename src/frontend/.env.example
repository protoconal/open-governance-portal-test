# .env.example — Template for required environment variables.
# Copy this file to .env and fill in the values.

# URL of the ASP.NET backend API (no trailing slash).
VITE_API_BASE_URL=http://localhost:5000
